language: cpp
compiler: clang

branches:
  only:
    - master

os:
  - linux

addons:
  apt:
    packages:
      - python3.5
      - gcc
      - make
      - libtool
      - automake
      - time

install:
  - python3.5 ./external.py
  - python3.5 ./make.py

script:
 - ./.build/linux64_clang/bin/cetech_testDebug

