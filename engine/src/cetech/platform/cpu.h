#pragma once

#include "celib/defines.h"

namespace cetech {
    namespace cpu {
        /*!
         * Get cpu core count.
         * \return Core count.
         */
        uint32_t core_count();
    }
}