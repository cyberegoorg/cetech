version: 0
blobs:
    - name: level
      guard_prefix: CETECH
      includes:
        - <celib/string/stringid.h>
        - <celib/math/types.h>
      values:
        - u32 entities_count
        - stringid64_t names[entities_count]
        - u32 offset[entities_count]
        - u8 data[*]
