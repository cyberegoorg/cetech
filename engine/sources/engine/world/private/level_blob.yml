version: 0
blobs:
    - name: level
      guard_prefix: CETECH
      includes:
        - <celib/string/stringid.h>
        - <celib/math/types.h>
      values:
        - u32 units_count
        - stringid64_t names[units_count]
        - u32 offset[units_count]
        - u8 data[*]
