#render_states:
#  default:
#    states:
#      color_write: "true"
#      alpha_write: "true"
#      depth_write: "true"
#      depth_func: "less"
#      cull_mode: "cw"

# Create on init



# Renderer layers
layer:
  default:
    - { name: "gbuffer", output: ["gbuffer0", "gbuffer1"] }

viewport:
  default:
    layer: default
